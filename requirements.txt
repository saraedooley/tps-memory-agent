# Databricks Apps runtime dependencies (flat pip list), trimmed to exactly what
# the app imports so pip resolves fast (no backtracking). databricks-langchain
# brings langchain-core + a compatible langgraph/psycopg; we don't import the
# heavyweight `langchain`/`langchain-community` meta-packages directly.
#
# Keep the [memory] extra: it provides AsyncDatabricksStore / AsyncCheckpointSaver.
databricks-langchain[memory]==0.19.0
unitycatalog-ai[databricks]==0.4.0
unitycatalog-langchain[databricks]==0.4.0
mlflow-skinny[databricks]==3.13.0
langgraph==1.0.5
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
