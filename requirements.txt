# Databricks Apps runtime dependencies (flat pip list).
# Pins match the tested enablement-lab stack. Do NOT drop the [memory] extra:
# without it, AsyncDatabricksStore / AsyncCheckpointSaver are not installed and
# the long-term + short-term memory layers will fail to import at runtime.
backoff==2.2.1
databricks-openai==0.15.0
databricks-agents==1.11.0
mlflow-skinny[databricks]==3.13.0
databricks-langchain[memory]==0.19.0
langchain-community==0.4.1
langchain==1.2.6
unitycatalog-langchain[databricks]==0.4.0
unitycatalog-ai[databricks]==0.4.0
databricks-sdk==0.94.0
langgraph==1.0.5
langgraph-prebuilt==1.0.5
langgraph-checkpoint==3.0.1
langgraph-checkpoint-postgres==3.0.1
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
# Used only by scripts/inspect_lakebase.py for the "memory as a governed asset" demo.
psycopg[binary,pool]==3.2.3
